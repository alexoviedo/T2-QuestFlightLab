# Scenes

`InputLab.unity` is generated by:

```text
QuestFlightLab.Editor.QuestProjectBootstrap.CreateInputLabScene
```

The scene was not generated in this run because Unity batchmode is blocked by editor license activation on this machine.

